// constants/config.ts

/**
 * General app logging (Game actions, lifecycle, etc.)
 */
export const ENABLE_LOGS = false;

/**
 * DEV-ONLY tooling:
 * - StatsEngine debug dumps
 * - RoundValidator
 * - Storage readback checks
 */
export const ENABLE_DEV_TOOLS = false;
